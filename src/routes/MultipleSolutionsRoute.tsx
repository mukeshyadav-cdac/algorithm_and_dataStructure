import React from 'react';
import { MultipleSolutionsDemo } from '../components/MultipleSolutionsDemo';

/**
 * Multiple Solutions Route Component
 * Dedicated route for the advanced multiple solutions and complexity analysis features
 */
export const MultipleSolutionsRoute: React.FC = () => {
  return (
    <div className="min-h-screen">
      <MultipleSolutionsDemo />
    </div>
  );
};
